-- تهيئة قاعدة البيانات
-- يتم تنفيذ هذا الملف تلقائياً عند أول تشغيل لـ PostgreSQL

-- إنشاء امتداد UUID إذا لم يكن موجوداً
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

-- ملاحظة: الجداول يتم إنشاؤها تلقائياً بواسطة SQLAlchemy
-- هذا الملف للتهيئة الإضافية إذا لزم الأمر
